import { CustomMinTableDirective } from './custom-min-table.directive';

describe('CustomMinTableDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomMinTableDirective();
    expect(directive).toBeTruthy();
  });
});
