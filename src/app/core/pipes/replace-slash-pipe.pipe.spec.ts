import { ReplaceSlashPipePipe } from './replace-slash-pipe.pipe';

describe('ReplaceSlashPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceSlashPipePipe();
    expect(pipe).toBeTruthy();
  });
});
