import { FormatoTemasPipe } from './formato-temas.pipe';

describe('FormatoTemasPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoTemasPipe();
    expect(pipe).toBeTruthy();
  });
});
